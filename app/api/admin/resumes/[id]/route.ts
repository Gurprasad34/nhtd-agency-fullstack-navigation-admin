import {NextResponse} from "next/server";import {isAdmin} from "@/lib/auth";import {prisma} from "@/lib/prisma";import {readFile} from "fs/promises";import path from "path";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){if(!(await isAdmin()))return new NextResponse("Unauthorized",{status:401});const {id}=await params;const row=await prisma.jobApplication.findUnique({where:{id:Number(id)}});if(!row)return new NextResponse("Not found",{status:404});const file=await readFile(path.join(process.cwd(),"uploads",row.resumeStoredName));return new NextResponse(file,{headers:{"Content-Type":"application/octet-stream","Content-Disposition":`attachment; filename="${row.resumeOriginalName.replace(/"/g,"")}"`}})}