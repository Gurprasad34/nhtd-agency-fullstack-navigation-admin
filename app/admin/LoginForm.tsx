"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function LoginForm(){const r=useRouter();const[s,setS]=useState({loading:false,error:""});async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setS({loading:true,error:""});const f=new FormData(e.currentTarget);const res=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f.entries()))});const d=await res.json();if(res.ok){r.refresh()}else setS({loading:false,error:d.error})}return <form onSubmit={submit} className="card mx-auto max-w-md space-y-5"><h1 className="text-3xl font-bold">Admin Login</h1><div><label className="label">Email</label><input name="email" type="email" className="field" required/></div><div><label className="label">Password</label><input name="password" type="password" className="field" required/></div><button className="btn-primary w-full">{s.loading?"Signing in...":"Sign In"}</button>{s.error&&<p className="text-red-600">{s.error}</p>}</form>}