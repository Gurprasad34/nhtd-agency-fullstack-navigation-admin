"use client";import {useState} from "react";
export default function ContactForm(){const[state,setState]=useState({loading:false,message:"",ok:false});async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState({loading:true,message:"",ok:false});const form=new FormData(e.currentTarget);const body=Object.fromEntries(form.entries());const res=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await res.json();setState({loading:false,message:data.message||data.error,ok:res.ok});if(res.ok)e.currentTarget.reset()}return <form onSubmit={submit} className="card space-y-5"><div className="grid gap-5 sm:grid-cols-2"><div><label className="label">Full name</label><input className="field" name="name" required/></div><div><label className="label">Email</label><input className="field" name="email" type="email" required/></div><div><label className="label">Phone</label><input className="field" name="phone"/></div><div><label className="label">Service interest</label><select className="field" name="service"><option value="">Select one</option><option>Service Coordination</option><option>ILST</option><option>General question</option></select></div></div><div><label className="label">Message</label><textarea className="field min-h-36" name="message" required/></div><button disabled={state.loading} className="btn-primary w-full">{state.loading?"Sending...":"Send Message"}</button>{state.message&&<p className={state.ok?"text-green":"text-red-600"}>{state.message}</p>}<p className="text-xs text-slate-500">Please do not include protected health information or medical documents.</p></form>}
